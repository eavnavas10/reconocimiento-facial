#Librerias a utilizar, las que no se encuentran listadas vienen en el paquete por defecto de python
#Grupo2 Ronald Godinez, Ernesto Villatoro, Jose Maldonado, Kenneth Herrera

# OpenCV
pip install opencv-python

# dlib
pip install dlib

# scipy (para usar distance de scipy.spatial)
pip install scipy

# matplotlib (para pyplot)
pip install matplotlib

# mtcnn (para el detector de rostros MTCNN)
pip install mtcnn

# tensorflow (para manejo de multiples datos)
pip install tensorflow

# numpy
pip install numpy

# instalar CMake (para compilar codigo escrito en c++ como es el caso de la libreria dlib)
cmake.exe

# Ejecutar el programa
python main.py